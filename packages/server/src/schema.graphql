# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

"""
The javascript `Date` as string. Type represents date and time as the ISO Date string.
"""
scalar DateTime

type Lesson {
  id: ID!
  start: DateTime!
  end: DateTime!
  comment: String!
  teacher: Teacher!
  student: Student
}

type Mutation {
  addLesson(teacherId: ID!, start: DateTime!, end: DateTime!, comment: String = "", repeatWeeks: Int!): Boolean!
  editLesson(id: ID!, studentId: ID, teacherId: ID!, start: DateTime!, end: DateTime!, comment: String!): Lesson!
  setComment(id: ID!, comment: String!): Lesson!
  deleteLesson(id: ID!, cancelReason: String): String!
  addStudent(name: String!, phone: String = "", emergencyName: String = "", emergencyRelation: String = "", emergencyPhone: String = "", detailedAddress: String = ""): Student!
  editStudent(id: ID!, name: String, phone: String, emergencyName: String, emergencyRelation: String, emergencyPhone: String, detailedAddress: String): Student!
  addTeacher(name: String!, hourPrice: Int!): Teacher!
  deleteTeacher(id: ID!): Boolean!
  editTeacher(id: ID!, name: String!, hourPrice: Int!): Teacher!
  logout: Boolean!
  login(username: String!, password: String!): User!
}

type Query {
  lessons: [Lesson!]!
  students: [Student!]!
  student(id: ID!): Student!
  teachers: [Teacher!]!
  me: User
  user(id: ID!): User!
  users: [User!]!
}

type Student {
  id: ID!
  name: String!
  phone: String!
  emergencyName: String!
  emergencyRelation: String!
  emergencyPhone: String!
  detailedAddress: String!
  bookedLessons: [Lesson!]!
}

type Teacher {
  id: ID!
  name: String!
  lessons: [Lesson!]
  hourPrice: Float!
}

type User {
  id: ID!
  email: String!
  role: UserRole!
  username: String
}

"""User Roles"""
enum UserRole {
  Admin
  User
  NewUser
}
